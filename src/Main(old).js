// import store from "./store/store";
// import { addUser, removeUser } from "./store/users/action";

function Main() {
  // store.dispatch(addUser(1, "John"));

  // console.log(store.getState());
  return (
    <>
      {/* {store.getState()[0] ? (
        <div>
          <h2>userId: {store.getState()[0].userId}</h2>
          <h2>token: {store.getState()[0].token}</h2>

          <button onClick={() => store.dispatch(removeUser(1))}>Remove</button>
        </div>
      ) : (
        <h2>No user</h2>
      )} */}
    </>
  );
}

export default Main;
